import { Callout } from "nextra-theme-docs";

# 备份与回滚

Mog 支持导出当前的配置，以及导入配置到当前的配置。

在导出配置时，Mog 会将当前的配置导出为一个 JSON 文件，该文件包含了所有的配置信息，包括：

- 文章 - `posts`
- 页面 - `pages`
- 评论 - `comments`
- 分类 - `categories`
- 用户信息 - `user`
- 友链 - `friends`

在导入配置时，Mog 会将导入的配置与当前的配置进行合并

- 如果导入的配置中包含了当前配置中不存在的配置项，则会将其添加到当前配置中
- 如果导入的配置中包含了当前配置中已存在的配置项，则会将其覆盖。

## 导出备份

<Callout type="info">
  为了保证数据的安全，Mog 不会将用户的密码导出到备份文件中。在全新导入用户时，Mog 会自动生成一个随机密码，您可以在导入完成后，通过 `用户` 页面，修改用户的密码。
</Callout>

1. 在 `Mog` 的 `备份` 页面，点击 `导出备份` 按钮，即可将当前的全部数据导出为一个 JSON 文件。
2. 在弹出的对话框中，点击 `下载` 按钮，即可下载该 JSON 文件。

## 导入备份

1. 在 `Mog` 的 `备份` 页面，点击 `导入备份` 按钮
2. 在弹出的对话框中，点击 `选择文件` 按钮，选择需要导入的 JSON 文件。

